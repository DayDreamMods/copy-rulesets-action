name: 'copy-rulesets-action'
description: "A GitHub action that copies rulesets from one repository to another."
inputs:
  token:
    description: 'The token to use.'
    require: false
    default: ${{ github.token }}
runs:
  using: 'node20'
  main: 'index.js'